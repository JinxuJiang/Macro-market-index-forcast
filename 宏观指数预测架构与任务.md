# 市场状态与宏观指数预测：架构及任务清单

> 状态：01数据层、02特征层已完成并验收，下一阶段为03模型数据集与标签层  
> 数据源：Tushare Pro  
> 建立日期：2026-08-02
> 最近更新：2026-08-12

## 1. 项目定位

本仓库是完整量化投研系统中的“市场状态模型”仓库。

三个仓库的职责边界：

```text
截面多因子模型：回答“买什么”
市场状态模型：回答“总体买多少”
组合投研系统：回答“每只股票买多少，并检查组合风险”
```

本项目不负责个股选择和个股权重优化。它负责预测未来一段时间的 A 股市场状态，并向组合系统发布市场分数、上涨概率和建议总仓位。

## 2. 第一阶段研究目标

第一版先解决一个核心问题：

> 使用 T 日收盘后已经可知的信息，预测未来 20 个交易日的指数收益，并在 T+1 日开盘形成可执行的总仓位信号。

暂定设置：

| 项目 | 暂定方案 | 状态 |
|---|---|---|
| 数据源 | Tushare Pro | 已确定 |
| 主预测周期 | 20 个交易日 | 已确定 |
| 信号时点 | T 日收盘后 | 已确定 |
| 执行时点 | T+1 日开盘 | 已确定 |
| 模型更新频率 | 每月一次 | 已确定 |
| 主指数/主标签 | 中证1000 `000852.SH` | 已确定 |
| 对照标签 | 沪深300 `000300.SH`、中证全指 `000985.CSI` | 已确定 |
| 基准模型 | 规则、线性模型、LightGBM | 已确定 |
| 深度模型 | CNN-GRU / CNN-LSTM | 后续研究 |
| 强化学习 | 暂不进入第一阶段 | 已确定 |

选择中证1000作为 V1 主标签的依据：当前截面模型 Top20 的典型总市值中位数约为 31 亿元，约 79% 的入选记录低于 50 亿元，具有明确的小盘风格；在 2023-10 至 2026-07 的现有实盘规则样本中，中证1000与 Top20 的日频相关性、解释力和下跌期相关性均高于上证指数。V1 使用沪深300和中证全指作为大盘/全市场对照，不让上证指数的重复信息进入正式特征表。

## 3. 可参考的截面仓库设计

参考仓库：

```text
C:\Users\蒋大王\Desktop\量化\截面多因子模型
```

计划复用的工程思想：

- YAML 配置驱动；
- 每次实验保存实际配置快照；
- 使用真实可执行的 open-to-open 标签；
- 训练、验证和预测之间设置标签隔离期；
- 固定周期模型，避免历史预测被后续重训覆盖；
- Freeze manifest 和增量更新；
- 保存模型、预测、特征重要性和汇总指标；
- 模型研究结果通过版本化发布目录对外输出。

不能直接照搬的内容：

- 截面数据构造器；
- 截面 Rank IC 评价体系；
- 3 年训练窗口；
- 季度固定模型；
- 截面 LightGBM 的现有超参数；
- 按股票分组的预测平滑逻辑。

市场模型需要改成单变量时间序列的数据集、月度固定模型和时间序列评价体系。

## 4. 建议项目架构

```text
市场状态模型/
├── 01数据/
│   ├── tushare_engine/
│   ├── raw/
│   │   ├── index/             # 指数行情
│   │   ├── stock_market/      # 市场宽度所需股票行情
│   │   ├── liquidity/         # 融资、资金和利率数据
│   │   └── macro/             # PMI、CPI、PPI、M2等
│   └── metadata/              # 交易日历、下载和更新时间记录
│
├── 02特征层/
│   ├── config/features.yaml
│   ├── feature_engine/
│   │   ├── common.py                   # 读取、通用公式与结果结构
│   │   ├── pit_aligner.py              # 按available_date做PIT对齐
│   │   ├── layer1_target.py            # 主指数自身状态
│   │   ├── layer2_macro_cross_asset.py # 资金、宏观与跨资产
│   │   ├── layer3_market_structure.py  # 市场结构与海外风险
│   │   └── engine.py                   # 编排、验收与原子发布
│   ├── build_features.py
│   ├── validate_features.py
│   └── data/processed/
│       ├── feature_table.parquet       # 中国交易日频、一日一行
│       └── feature_lineage.parquet     # PIT来源与数据年龄审计表
│
├── 03模型训练层/
│   ├── configs/
│   ├── dataset/
│   │   ├── label_constructor.py
│   │   └── monthly_splitter.py
│   ├── models/
│   │   ├── baseline.py
│   │   ├── lightgbm_model.py
│   │   └── sequence_model.py
│   ├── training/
│   │   └── monthly_trainer.py
│   └── experiments/
│
├── 04回测层/
│   ├── exposure_mapper.py
│   ├── market_timing_backtest.py
│   └── metrics.py
│
├── 05输出层/
│   └── exports/
│       ├── releases/
│       └── current.json
│
├── validation/
├── docs/
├── tests/
└── README.md
```

第一阶段只创建实际需要的目录和代码，不为了架构完整提前创建大量空模块。

## 5. 数据与时间规范

### 5.1 时点定义

```text
T 日收盘后：计算特征并产生预测
T+1 日开盘：执行仓位调整
T+1 至 T+21：20个交易日持有期
```

所有特征必须在 T 日收盘后真实可知。

### 5.2 宏观数据 PIT 规则

低频宏观数据不能按统计月份直接填充到当月月初，必须记录：

```text
period_date     # 数据所属统计期
publish_date    # 实际公布日期
available_date  # 策略允许使用的首个交易日
```

如果没有可靠的日内发布时间，保守地从公布日后的首个交易日开始使用。

### 5.3 数据存储规则

- 原始数据与加工数据分开保存；
- 原始接口结果尽量保留，不在原地覆盖字段含义；
- 每张数据表记录数据源、抓取时间和覆盖日期；
- 特征表使用交易日作为唯一索引，一日一行；
- 特征缩放、缺失值处理和模型拟合只能使用当期训练集信息。

## 6. 标签设计

### 6.1 主回归标签

```text
target_index = 000852.SH  # 中证1000（Tushare代码）
ret_20d_o2o(T) = open_000852.SH[T+21] / open_000852.SH[T+1] - 1
```

对应真实的月度调仓执行时点。

LightGBM 回归模型预测：

```text
expected_return_20d
```

### 6.2 派生分类标签

```text
up_20d(T) = 1(ret_20d_o2o(T) > 0)
```

LightGBM 分类模型输出：

```text
up_probability_20d
```

回归和分类使用同一份未来收益构造，避免出现两套互相冲突的标签口径。

### 6.3 诊断标签

第一阶段可以同步计算但暂不作为主训练目标：

```text
realized_vol_20d
max_drawdown_20d
```

用于分析预测上涨是否伴随过高波动或较大路径回撤。暂不建设多任务网络。

## 7. V1正式数据与变量

V1 使用 12 类有效原始数据生成 39 个模型特征，另使用 1 份中国交易日历完成时间对齐。原始数据数量与模型特征数量不是同一个概念：一份指数日线可以派生多种收益、波动、趋势和回撤特征。第一版优先保证时点正确、经济含义明确和结果可解释。

### 7.1 第一层：主指数自身状态

原始数据为中证1000 `000852.SH` 的开盘、最高、最低、收盘、成交量和成交额。正式保留 13 个特征：

```text
ret_5d
ret_20d
ret_60d
ret_120d

vol_20d
vol_60d

price_to_ma20
price_to_ma60
price_to_ma250

drawdown_20d
drawdown_250d

intraday_range_20d
amount_ratio_20d
```

明确删除 `ret_10d`、`vol_5d`、`price_to_ma120`、`drawdown_60d`、`intraday_range_5d` 和 `amount_ratio_5d`。不直接把指数绝对点位输入模型，只使用收益率、比例和滚动状态变量。

### 7.2 第二层：资金、宏观与跨资产

#### 7.2.1 短期利率与杠杆资金

| 原始数据 | Tushare接口/字段 | V1衍生变量 | 可用时点与状态 |
|---|---|---|---|
| 1个月Shibor | `shibor.1m` | 水平、20日变化 | 可用且已下载；T日收盘后可用 |
| 沪深融资余额 | `margin.rzye` | 沪深合计余额、20日变化率 | 可用且已下载；T日数据从T+1交易日开始使用 |

Shibor 只保留1个月期限，不同时加入隔夜、1周、3个月等高度相关期限，也不在V1加入LPR。融资余额为保持长历史口径暂不合并北交所。

#### 7.2.2 经济增长与通胀

| 经济主题 | 原始数据 | Tushare接口/字段 | V1衍生变量 | 保守可用日规则 |
|---|---|---|---|---|
| 增长 | 制造业PMI | `cn_pmi.PMI010000` | `PMI-50`、PMI月度变化 | 统计期下一自然月的最后一个中国交易日开始使用 |
| 通胀 | 全国CPI | `cn_cpi.nt_yoy` | CPI同比、CPI同比月度变化 | 统计期下一自然月的最后一个中国交易日开始使用 |

当前Token实测可以获取PMI和CPI。考虑到历史发布日期表不完整且维护成本较高，V1不还原真实发布日期，统一使用上述简单、保守的PIT规则。该规则不依赖具体年份，并在数据中保留 `period_date`、`publish_date`（当前为空）、`available_date` 和规则来源。交易日历预取到当年年末，用于准确识别每月最后一个交易日，但模型特征仍只生成到指定截止日。

GDP虽可通过 `cn_gdp` 获取，但季度频率过低、发布滞后且可能修订，不进入V1正式特征。PPI、M1/M2和社融可获取，但仅作为V1.1消融实验候选。工业增加值和失业率不进入V1。

#### 7.2.3 汇率与黄金

| 原始数据 | Tushare接口/代码 | V1衍生变量 | 注意事项 |
|---|---|---|---|
| 美元兑离岸人民币 | `fx_daily`, `USDCNH.FXCM` | 5/20日收益、20日波动 | 按中国T日收盘前已经完成的行情做as-of对齐 |
| 上海金 | `sge_daily`, `Au99.99` | 20日收益、20日波动 | 日行情含前夜盘及当日日盘，需明确T日收盘时可用口径 |

当前Token实测两项均可获取。原油主力连续从V1删除，跨资产层也不扩充铜、白银和其他商品。

#### 7.2.4 长期利率与信用利差

Tushare存在 `yc_cb` 接口，但当前Token实测无访问权限。因此V1删除10年国债收益率、`10Y-1Y`期限利差和信用利差。当前利率层只使用可持续获取的1个月Shibor作为短期资金价格代理；以后获得可靠长期利率数据时再做独立增量实验。

### 7.3 第三层：市场结构与海外风险偏好

#### 7.3.1 国内辅助指数

```text
沪深300    000300.SH
中证500    000905.SH
中证全指   000985.CSI
```

三者均使用 `index_daily`，已经下载并验证2010年至今的完整日线。候选特征为各指数20日收益和20日波动，以及“中证1000减沪深300”和“中证1000减中证全指”的20日相对收益。上证指数行情虽已下载，但不进入V1正式特征；创业板指和上证50也暂不使用。

#### 7.3.2 美国辅助指数

```text
标普500指数       SPX
纳斯达克综合指数  IXIC
```

两者通过 `index_global` 获取，当前Token实测可用。候选特征为各自20日收益、20日波动以及“纳斯达克减标普500”的20日相对收益。

中国T日收盘时，美国T日尚未收盘，因此T日信号只能使用当时已经完成的最近一个美国交易日，通常为美国T-1日。必须按实际可用时间做as-of对齐，禁止按相同日历日期直接合并。

### 7.4 V1原始数据总表

| 层级 | 原始数据 | 接口/代码 | 当前状态 | V1决策 |
|---|---|---|---|---|
| 时间基础 | 中国交易日历 | `trade_cal` | 已下载 | 正式使用但不作为模型特征 |
| 第一层 | 中证1000 | `index_daily`, `000852.SH` | 已下载 | 正式使用 |
| 第三层 | 沪深300 | `index_daily`, `000300.SH` | 已下载 | 正式使用 |
| 第三层 | 中证500 | `index_daily`, `000905.SH` | 已下载 | 正式使用 |
| 第三层 | 中证全指 | `index_daily`, `000985.CSI` | 已下载 | 正式使用 |
| 第三层 | 标普500 | `index_global`, `SPX` | 已下载 | 正式使用 |
| 第三层 | 纳斯达克综合指数 | `index_global`, `IXIC` | 已下载 | 正式使用 |
| 第二层 | 1个月Shibor | `shibor.1m` | 已下载 | 正式使用 |
| 第二层 | 沪深融资余额 | `margin.rzye` | 已下载 | 正式使用，滞后1个交易日 |
| 第二层 | 制造业PMI | `cn_pmi.PMI010000` | 已下载 | 正式使用，保守PIT |
| 第二层 | 全国CPI | `cn_cpi.nt_yoy` | 已下载 | 正式使用，保守PIT |
| 第二层 | 离岸人民币 | `fx_daily`, `USDCNH.FXCM` | 已下载 | 正式使用，滞后至下一中国交易日 |
| 第二层 | 上海金 | `sge_daily`, `Au99.99` | 已下载 | 正式使用，滞后至下一中国交易日 |

V1共13类原始数据，其中交易日历只负责时间对齐，其余12类数据已生成39个模型特征。13类数据均已接入统一下载命令并完成落盘；截至2026-08-12的数据层完整验收结果为14项通过、0警告、0失败。

### 7.4.1 数据层完成状态（2026-08-12）

- 国内四个指数均覆盖 `20100104..20260812`，各4033行；
- SPX和IXIC均覆盖 `20100104..20260811`，各4176行；
- 1个月Shibor覆盖 `20100104..20260812`，4123行；
- 沪深两融覆盖 `20100331..20260811`，8799行；
- 制造业PMI覆盖 `200501..202607`，259行；
- 全国CPI覆盖 `195112..202607`，511行；
- USDCNH覆盖 `20120218..20260811`，4392行；
- Au99.99覆盖 `20100104..20260812`，4033行；
- `data_availability.parquet` 包含12个有效数据集、41775条PIT可用日期记录；
- 所有原始数据主键重复数和空主键数均为0；所有 `available_date` 均为中国交易日，没有数据早于允许时点进入模型；
- 模拟逻辑测试4项通过，小样本真实Tushare接口测试2项通过；
- 日常运行命令为 `python 01数据/download_data.py`，命令自动增量更新、生成PIT表并执行完整验收。

### 7.4.2 特征层完成状态（2026-08-12）

- 输出 `4033 × 40` 的 `feature_table.parquet`：1列 `trade_date` 加39个正式特征，覆盖 `20100104..20260812`；
- 三层各13个特征；特征先在各自原生频率上计算，再按 `available_date` 映射到中国交易日，月频数据只做结构性前向持有；
- 输出48396条长表审计记录 `feature_lineage.parquet`，逐日保存来源数据日、统计期、可用日和交易日龄；
- 全部特征首次同时有效日期为 `20120319`，由USDCNH历史起点决定；最新交易日39个特征均非空；
- 特征层不做依赖样本统计量的缺失值填充、异常值缩尾和标准化，这些步骤必须在03层的每个训练折内拟合；
- 公式、PIT可用性和截断重算因果性测试全部通过；保存结果独立验收为12项通过、0警告、0失败；
- 构建命令为 `python 02特征层/build_features.py`，只读验收命令为 `python 02特征层/validate_features.py`。

### 7.5 V1暂不使用的数据

- 上证指数特征、创业板指、上证50和更多海外指数；
- PPI、GDP、M1/M2、社融、工业增加值和失业率；
- 原油、铜、白银及其他商品；
- 多期限Shibor和LPR；
- 当前Token无权限的中国国债期限结构、长期利率和信用利差；
- 全A市场宽度和市值分组。

其中PPI、M1/M2、社融、全A市场宽度和长期利率可保留为后续消融实验候选，但不进入第一版正式特征表。

## 8. Walk-forward 设计

暂定第一版：

```yaml
walk_forward:
  train_window: 8Y
  valid_window: 1Y
  test_window: 1M
  step: 1M
  gap: 21
  model_update: monthly
```

规则：

- 每个自然月训练或冻结一个固定模型；
- 当月预测不得因未来数据加入而变化；
- 训练与验证、验证与预测之间都保留 21 个交易日隔离；
- 第一版使用滚动 8 年训练窗口；
- 后续将滚动窗口与 expanding window 做正式对比；
- 深度学习和传统模型必须使用完全相同的预测折；
- 深度学习的 scaler 只能在每个 fold 的训练集上拟合。

## 9. 模型与基准

计划比较：

```text
基准0：始终保持100%股票仓位
基准1：简单均线规则
基准2：逻辑回归 / Elastic Net
模型1：LightGBM回归
模型2：LightGBM分类
模型3：CNN-GRU / CNN-LSTM
模型4：简单模型融合
```

第一阶段先完成基准和 LightGBM；深度模型在数据、标签、切分和回测全部验收后加入。

## 10. 评价指标

### 10.1 预测指标

回归：

```text
MAE
RMSE
Pearson相关系数
Spearman相关系数
```

分类：

```text
AUC
Brier Score
方向准确率
混淆矩阵
```

### 10.2 仓位策略指标

```text
年化收益率
年化波动率
夏普比率
最大回撤
换手率
上涨参与率
下跌捕获率
不同市场阶段表现
```

最终是否采用某个模型，以严格样本外仓位回测为主，不能只看预测误差或方向准确率。

## 11. 对外输出协议

市场模型计划发布：

```text
date
target_index
expected_return_20d
up_probability_20d
market_score
target_exposure
model_name
model_version
asof_time
```

其中：

- `expected_return_20d`：未来20日预期收益；
- `up_probability_20d`：未来20日上涨概率；
- `market_score`：统一标准化市场分数；
- `target_exposure`：建议股票总仓位；
- `asof_time`：本条预测使用数据的截止时点。

预测文件与 manifest 通过 `05输出层/exports/releases/` 发布，组合投研系统只读取正式发布结果。

## 12. 实施任务清单

### 阶段 A：设计确认

- [x] 确定使用 Tushare Pro；
- [x] 确定未来20个交易日为第一预测周期；
- [x] 确定 T 日收盘产生信号、T+1 日开盘执行；
- [x] 确定月度更新模型；
- [x] 确定先建立规则、线性和 LightGBM 基准；
- [x] 确定中证1000 `000852.SH` 为V1主指数；
- [x] 确定V1三层变量框架及精简原始数据清单；
- [ ] 确定市场分数到总仓位的映射方式。

### 阶段 B：数据层

- [x] 数据层最终验收：13类原始数据可一键增量更新，14项检查全部通过；
- [x] 设计 Tushare 下载配置和数据目录；
- [x] 下载并验证目标指数和辅助指数行情；
- [x] 下载交易日历、Shibor和融资数据；
- [x] 设计与截面仓库行情宽表的只读适配器；
- [x] 建立下载日志和数据覆盖检查；
- [x] 建立日频数据日期/可用日期映射表；
- [x] 扩展下载器：`SPX`、`IXIC` 国际指数；
- [x] 扩展下载器：制造业PMI与全国CPI；
- [x] 扩展下载器：USDCNH与Au99.99；
- [x] 为PMI落实“统计期次月最后一个中国交易日”的保守可用日规则；
- [x] 为CPI落实“统计期次月最后一个中国交易日”的保守可用日规则；
- [x] 按三层信息结构重组原始数据目录，并在迁移验收后删除旧目录与旧可用日表；
- [x] 建立模拟逻辑测试和小样本真实Tushare接口测试；
- [x] 确定国债期限结构、长期利率和信用利差不进入V1；
- [x] 确定原油主力连续不进入V1。

### 阶段 C：特征层

- [x] 实现指数自身量价特征；
- [x] 实现国内外跨指数结构特征；
- [x] 实现1个月Shibor、融资余额、PMI和CPI特征；
- [x] 实现USDCNH和黄金跨资产特征；
- [x] 独立实现PIT对齐，并保留逐特征来源日期和数据年龄；
- [x] 完成特征时点、公式与截断重算未来函数审计；
- [x] 输出统一 `feature_table.parquet` 和 `feature_lineage.parquet`。

### 阶段 D：模型数据集、Walk-forward 与基准模型

- [ ] 实现20日 open-to-open主标签；
- [ ] 实现上涨分类标签和诊断标签；
- [ ] 在每个训练折内拟合缺失值、异常值和标准化参数；
- [ ] 实现 Monthly PIT splitter；
- [ ] 验证21日隔离区间；
- [ ] 实现始终满仓和均线规则基准；
- [ ] 实现逻辑回归/Elastic Net；
- [ ] 实现 LightGBM 回归；
- [ ] 实现 LightGBM 分类；
- [ ] 保存每月模型、配置、预测和特征重要性；
- [ ] 实现 Freeze 增量更新。

### 阶段 E：仓位回测

- [ ] 设计市场分数标准化方法；
- [ ] 比较离散仓位与连续仓位映射；
- [ ] 加入仓位平滑和最低调仓阈值；
- [ ] 完成指数择时回测；
- [ ] 评估上涨参与率和下跌捕获率；
- [ ] 以中证1000为主标签，对比沪深300与中证全指标签；
- [ ] 测试市场信号对截面Top20组合的实际增益。

### 阶段 F：深度模型与扩展研究

- [ ] 设计统一序列数据接口；
- [ ] 实现GRU/LSTM基准；
- [ ] 实现CNN-GRU或CNN-LSTM；
- [ ] 与LightGBM使用相同Walk-forward预测折比较；
- [ ] 加入严格PIT的低频宏观变量；
- [ ] 研究简单模型融合；
- [ ] 在监督学习方案稳定后再评估Bandit或RL。

## 13. 当前待讨论问题

02特征层已经完成。下一步进入03模型数据集层，编码前依次确认：

1. 冻结主标签 `open[T+21] / open[T+1] - 1` 的精确区间，并确认样本归属日仍为信号日T；
2. 冻结上涨分类标签，以及未来20日波动和最大回撤诊断标签的精确计算区间；
3. 确认首版采用滚动8年训练、1年验证、1个月测试及21交易日标签隔离；
4. 设计训练折内预处理顺序：仅用训练集统计量拟合缺失值、异常值和标准化参数，再原样应用到验证集和测试集；
5. 明确特征自然缺失的处理范围，不回填数据源尚未出现的整段历史；
6. 先完成规则、线性模型和LightGBM基准，再讨论特征扩展、序列模型与仓位映射。

下一步不扩展数据源，也不随意增加特征。先讨论并确认《03模型数据集、标签与Walk-forward定义》，用户确认后再开始编码。
